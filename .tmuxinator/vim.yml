# ~/.tmuxinator/vim.yml

name: vim
# root: ~/.config/nvim/

# start session with this window open
startup_window: editor
startup_pane: "1"

windows:
  - editor: cd ~/.config/nvim/ && nvim .
  - bash:
