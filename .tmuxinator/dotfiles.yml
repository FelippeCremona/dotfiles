# ~/.tmuxinator/dotfiles.yml

name: dotfiles
# root: ~/

# start session with this window open
startup_window: vim

windows:
  - vim: nvim ~/.config/nvim/ 
  - bash: nvim ~/.bashrc
  - tmux: nvim ~/.tmux.conf
  - starship: nvim ~/.config/starship.toml 
  - tmuxinator: nvim ~/.tmuxinator/
  - bash: 
